// count total characters
// count punctuation and whitespace separately
// have options to include/exclude those separate counts
// support very large inputs, but handle it gracefully
// good UI
// histogram of characters (possibly using chart.js)
